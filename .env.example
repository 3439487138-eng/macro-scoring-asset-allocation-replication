# The bundled project uses a local, user-supplied snapshot and requires no API
# credentials. This optional override must point to a directory containing the
# four exact input filenames documented in data/raw/README.md.
MACRO_STRATEGY_DATA_DIR=
